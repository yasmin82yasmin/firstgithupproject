package yasmin;

public class ComparissonOperators {
    public static void main(String[] args) {
        /*
        == is equal too
        != is not equal
        <= less than or equal
        >= greater than equal
        < less than
        > greater than
        || or
        && and
         */

    }
}
